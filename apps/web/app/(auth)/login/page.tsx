export default function LoginPage() {
  return (
    <main
      style={{
        minHeight: "100vh",
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
        justifyContent: "center",
        fontFamily: "sans-serif",
        gap: "1rem",
      }}
    >
      <h1>Login</h1>
      <p>Autenticação será implementada em fase futura.</p>
      <a href="/">Voltar</a>
    </main>
  );
}

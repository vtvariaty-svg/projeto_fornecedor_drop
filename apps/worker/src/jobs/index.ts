// Job registry — add BullMQ job processors here in future phases.
// Example:
//   import { Queue, Worker } from 'bullmq';
//   export const orderQueue = new Queue('orders', { connection });

export {};
